PUSH AN EXISTING PROJECT TO NEW REPO

setting up new repository and adding existing project

~ git init (initiates git)

~ git add .

~ git commit -m "First Commit"

~ git remote add origin {remote repo url}

~ git push -u origin master
